function sigmoid(x: number): number {
  return 0;
}

function sigmoid_deriv(x: number): number {
  return 0;
}

function relu(x: number): number {
  return 0;
}

function relu_deriv(x: number): number {
  return 0;
}
